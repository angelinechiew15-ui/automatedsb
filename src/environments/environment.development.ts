// Development overrides — used by `ng serve` / `npm start`.
export const environment = {
  production: false,
  // .NET API dev URL (see AutomatedSb.Api/Properties/launchSettings.json).
  // Use https://localhost:7129 if you prefer HTTPS during dev.
  apiBase: 'http://localhost:5019/api',
};
